package com.leyde.backend.application.service;

/**
 * Marker interface for application services (use cases).
 * Implementations will be provided in the service layer when features are added.
 */
public interface ApplicationService {
}
