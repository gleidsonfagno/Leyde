UI primitives and shared design tokens

This folder contains low-level, reusable UI primitives that follow the project's Design System.

Guidelines:
- Prefer server components for static content and composition
- Use client components only when interactivity is required ("use client")
- Keep components small and composable
- Add stories/tests when implementing real components
